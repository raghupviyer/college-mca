export const sum = (x,y) => x + y
export default difference = (x,y) => x - y 
// can have only one default export per module, 
//can be renamed at the importing page at the import statement
export const product = (x,y) => x * y 

//export default sum
//export default {sum, difference, product}